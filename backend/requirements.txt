# FastAPI and web server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Pydantic for data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# CORS is handled by FastAPI's CORSMiddleware (included in fastapi)

# Document processing
python-docx>=1.1.0
pandas>=2.1.0
openpyxl>=3.1.0

# Embeddings and vector database
# Note: PyTorch CPU-only is installed in Dockerfile to avoid NVIDIA GPU libraries
# (cuDNN, NCCL, cuSPARSELt) - see Dockerfile for CPU-only installation
# torch is installed separately in Dockerfile before this requirements.txt
sentence-transformers>=2.2.2
qdrant-client>=1.16.0  # Updated for better async support and bug fixes

# LangChain (optional, for RAG orchestration)
langchain-core>=0.1.0
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# HTTP client for Ollama
# Note: httpx version pinned to avoid "client has been closed" issues with huggingface_hub
httpx==0.27.0  # Pinned to avoid compatibility issues with huggingface_hub
requests>=2.31.0

# Environment and configuration
python-dotenv>=1.0.0

