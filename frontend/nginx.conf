server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (client-side routes: /upload, /documents, /projects, …)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
